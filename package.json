{
  "name": "job-tracker-vite",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:ext": "vite build && node ./scripts/generate-extension.js",
    "generate:icons": "node ./scripts/generate-icons.js"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "sharp": "^0.33.0",
    "vite": "^5.0.0"
  }
}

